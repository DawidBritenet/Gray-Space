({
    doInit: function (component, event, helper) {
        helper.getCart(component, event);
    },

    startCartFlow: function (component, event, helper) {
        helper.startCartFlow(component, event);
    }
});